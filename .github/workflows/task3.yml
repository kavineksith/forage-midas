# =============================================================================
# .github/workflows/task3.yml  (v3 — fully corrected)
#
# Fixes vs previous version:
#   1. DatabaseConduit heredoc now includes the save(UserRecord) method
#      that UserPopulator calls at line 21
#   2. Parse + summary steps use "if: always()" so task3_answer.md is
#      always written even when the build or tests fail
#   3. Build step has continue-on-error: true so downstream steps run
# =============================================================================

name: "Midas Task 3 — H2 Database & waldorf Balance"

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  task-three:
    name: "JPA integration → Run TaskThreeTests → Report waldorf balance"
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:

      # ── 1. Checkout ────────────────────────────────────────────────────────
      - name: "Checkout repository"
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ── 2. Java 17 ─────────────────────────────────────────────────────────
      - name: "Set up Java 17"
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: "maven"

      # ── 3. Ensure pom.xml dependencies ────────────────────────────────────
      - name: "Ensure required dependencies in pom.xml"
        run: |
          python3 << 'PYEOF'
          import xml.etree.ElementTree as ET

          POM = "pom.xml"
          NS  = "http://maven.apache.org/POM/4.0.0"
          TAG = f"{{{NS}}}"
          ET.register_namespace("", NS)

          tree = ET.parse(POM)
          root = tree.getroot()
          deps_el = root.find(f"{TAG}dependencies") or ET.SubElement(root, f"{TAG}dependencies")
          existing = {el.findtext(f"{TAG}artifactId","") for el in deps_el.findall(f"{TAG}dependency")}

          required = [
              dict(groupId="org.springframework.boot",  artifactId="spring-boot-starter-data-jpa", version="3.2.5"),
              dict(groupId="org.springframework.boot",  artifactId="spring-boot-starter-web",       version="3.2.5"),
              dict(groupId="org.springframework.kafka", artifactId="spring-kafka",                  version="3.1.4"),
              dict(groupId="com.h2database",            artifactId="h2",                            version="2.2.224", scope="runtime"),
              dict(groupId="org.springframework.boot",  artifactId="spring-boot-starter-test",      version="3.2.5",   scope="test"),
              dict(groupId="org.springframework.kafka", artifactId="spring-kafka-test",             version="3.1.4",   scope="test"),
              dict(groupId="org.testcontainers",        artifactId="kafka",                         version="1.19.1",  scope="test"),
          ]
          for dep in required:
              if dep["artifactId"] not in existing:
                  el = ET.SubElement(deps_el, f"{TAG}dependency")
                  for k, v in dep.items():
                      ET.SubElement(el, f"{TAG}{k}").text = v
                  print(f"  [added] {dep['artifactId']}")
              else:
                  print(f"  [ok]    {dep['artifactId']}")
          tree.write(POM, xml_declaration=True, encoding="UTF-8")
          PYEOF

      # ── 4. Write application.yml ───────────────────────────────────────────
      - name: "Write application.yml (H2 + JPA + kafka-topic)"
        run: |
          APP="src/main/resources/application.yml"
          mkdir -p "$(dirname "$APP")"
          cat > "$APP" << 'YMLEOF'
          general:
            kafka-topic: trader-updates

          spring:
            datasource:
              url: jdbc:h2:mem:midasdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
              driver-class-name: org.h2.Driver
              username: sa
              password:
            jpa:
              database-platform: org.hibernate.dialect.H2Dialect
              hibernate:
                ddl-auto: create-drop
              show-sql: false
            h2:
              console:
                enabled: true
                path: /h2-console
          YMLEOF
          python3 -c "
          import textwrap, pathlib
          p = pathlib.Path('$APP')
          p.write_text(textwrap.dedent(p.read_text()))
          "
          echo "--- application.yml ---"
          cat "$APP"

      # ── 5. Discover package and source directory ───────────────────────────
      - name: "Discover project layout"
        id: discover
        run: |
          python3 << 'PYEOF'
          import os, subprocess
          from pathlib import Path

          result = subprocess.run(
              ["grep", "-rl", "@SpringBootApplication", "src/main/java"],
              capture_output=True, text=True
          )
          main_file = Path(result.stdout.strip().splitlines()[0])
          java_dir  = main_file.parent

          pkg_line = next(
              l for l in main_file.read_text().splitlines()
              if l.strip().startswith("package ")
          )
          package = pkg_line.strip().removeprefix("package ").removesuffix(";").strip()

          print(f"Package  : {package}")
          print(f"Java dir : {java_dir}")

          with open(os.environ["GITHUB_OUTPUT"], "a") as f:
              f.write(f"package={package}\n")
              f.write(f"java_dir={java_dir}\n")
          PYEOF

      # ── 6. Remove conflicting scaffold listener files ──────────────────────
      - name: "Remove conflicting listener files"
        env:
          JAVA_DIR: ${{ steps.discover.outputs.java_dir }}
        run: |
          COMPONENT="${JAVA_DIR}/component"
          rm -f "${COMPONENT}/TransactionListener.java"
          rm -f "${COMPONENT}/TransactionKafkaListener.java"
          echo "Listener cleanup done. Remaining component files:"
          ls -1 "${COMPONENT}/" 2>/dev/null || echo "(empty)"

      # ── 7. Write TransactionRecord.java ───────────────────────────────────
      - name: "Write entity/TransactionRecord.java"
        env:
          PACKAGE: ${{ steps.discover.outputs.package }}
          JAVA_DIR: ${{ steps.discover.outputs.java_dir }}
        run: |
          mkdir -p "${JAVA_DIR}/entity"
          cat > "${JAVA_DIR}/entity/TransactionRecord.java" << 'JAVAEOF'
          package PLACEHOLDER.entity;

          import jakarta.persistence.*;

          @Entity
          @Table(name = "transaction_record")
          public class TransactionRecord {

              @Id
              @GeneratedValue(strategy = GenerationType.IDENTITY)
              private long id;

              @ManyToOne(fetch = FetchType.LAZY, optional = false)
              @JoinColumn(name = "sender_id", nullable = false)
              private UserRecord sender;

              @ManyToOne(fetch = FetchType.LAZY, optional = false)
              @JoinColumn(name = "recipient_id", nullable = false)
              private UserRecord recipient;

              @Column(nullable = false)
              private float amount;

              protected TransactionRecord() {}

              public TransactionRecord(UserRecord sender, UserRecord recipient, float amount) {
                  this.sender    = sender;
                  this.recipient = recipient;
                  this.amount    = amount;
              }

              public long       getId()        { return id; }
              public UserRecord getSender()    { return sender; }
              public UserRecord getRecipient() { return recipient; }
              public float      getAmount()    { return amount; }
          }
          JAVAEOF
          sed -i "s|PLACEHOLDER|${PACKAGE}|g" "${JAVA_DIR}/entity/TransactionRecord.java"
          echo "TransactionRecord.java written"

      # ── 8. Write TransactionRepository.java ───────────────────────────────
      - name: "Write repository/TransactionRepository.java"
        env:
          PACKAGE: ${{ steps.discover.outputs.package }}
          JAVA_DIR: ${{ steps.discover.outputs.java_dir }}
        run: |
          mkdir -p "${JAVA_DIR}/repository"
          cat > "${JAVA_DIR}/repository/TransactionRepository.java" << 'JAVAEOF'
          package PLACEHOLDER.repository;

          import PLACEHOLDER.entity.TransactionRecord;
          import org.springframework.data.jpa.repository.JpaRepository;
          import org.springframework.stereotype.Repository;

          @Repository
          public interface TransactionRepository extends JpaRepository<TransactionRecord, Long> {
          }
          JAVAEOF
          sed -i "s|PLACEHOLDER|${PACKAGE}|g" "${JAVA_DIR}/repository/TransactionRepository.java"
          echo "TransactionRepository.java written"

      # ── 9. Write DatabaseConduit.java ──────────────────────────────────────
      #
      # IMPORTANT: includes save(UserRecord) which UserPopulator calls at line 21.
      # Without this method the build fails with a missing-symbol error.
      # Also uses null checks (not Optional) because UserRepository.findById(long)
      # returns UserRecord directly in this scaffold.
      - name: "Write component/DatabaseConduit.java"
        env:
          PACKAGE: ${{ steps.discover.outputs.package }}
          JAVA_DIR: ${{ steps.discover.outputs.java_dir }}
        run: |
          mkdir -p "${JAVA_DIR}/component"
          cat > "${JAVA_DIR}/component/DatabaseConduit.java" << 'JAVAEOF'
          package PLACEHOLDER.component;

          import PLACEHOLDER.entity.TransactionRecord;
          import PLACEHOLDER.entity.UserRecord;
          import PLACEHOLDER.foundation.Transaction;
          import PLACEHOLDER.repository.TransactionRepository;
          import PLACEHOLDER.repository.UserRepository;
          import org.slf4j.Logger;
          import org.slf4j.LoggerFactory;
          import org.springframework.stereotype.Component;
          import org.springframework.transaction.annotation.Transactional;

          /**
           * Central database gateway for Midas Core.
           *
           * Exposes two methods:
           *  - save(UserRecord)     : direct persistence used by UserPopulator to seed test data
           *  - process(Transaction) : validates a transaction and persists it if all rules pass
           */
          @Component
          public class DatabaseConduit {

              private static final Logger log = LoggerFactory.getLogger(DatabaseConduit.class);

              private final UserRepository        userRepository;
              private final TransactionRepository transactionRepository;

              public DatabaseConduit(UserRepository userRepository,
                                     TransactionRepository transactionRepository) {
                  this.userRepository        = userRepository;
                  this.transactionRepository = transactionRepository;
              }

              /**
               * Persists a UserRecord directly.
               * Called by UserPopulator to load seed users from test data files
               * before TaskThreeTests (and later tasks) run.
               */
              @Transactional
              public void save(UserRecord user) {
                  userRepository.save(user);
                  log.info("User saved: id={} name={} balance={}", user.getId(), user.getName(), user.getBalance());
              }

              /**
               * Validates and persists a transaction.
               *
               * Rules (all must pass):
               *  1. senderId    → existing UserRecord (null check; repo returns UserRecord directly)
               *  2. recipientId → existing UserRecord
               *  3. sender.balance >= transaction.amount
               *
               * On success: persist TransactionRecord, debit sender, credit recipient.
               * On failure: silently discard, no DB state modified.
               *
               * @Transactional: the three writes are one atomic unit — all succeed or all roll back.
               */
              @Transactional
              public void process(Transaction transaction) {

                  // Rule 1 — sender exists?
                  UserRecord sender = userRepository.findById(transaction.getSenderId());
                  if (sender == null) {
                      log.warn("Discarding — sender {} not found", transaction.getSenderId());
                      return;
                  }

                  // Rule 2 — recipient exists?
                  UserRecord recipient = userRepository.findById(transaction.getRecipientId());
                  if (recipient == null) {
                      log.warn("Discarding — recipient {} not found", transaction.getRecipientId());
                      return;
                  }

                  // Rule 3 — sufficient balance?
                  if (sender.getBalance() < transaction.getAmount()) {
                      log.warn("Discarding — sender {} balance {} < amount {}",
                              sender.getId(), sender.getBalance(), transaction.getAmount());
                      return;
                  }

                  // All valid: persist and update balances
                  transactionRepository.save(
                          new TransactionRecord(sender, recipient, transaction.getAmount()));

                  sender.setBalance(sender.getBalance() - transaction.getAmount());
                  recipient.setBalance(recipient.getBalance() + transaction.getAmount());
                  userRepository.save(sender);
                  userRepository.save(recipient);

                  // Tag both sides so the CI parser finds waldorf regardless of role
                  log.info("WALDORF_BALANCE_CHECK: user={} balance={}", sender.getName(),    sender.getBalance());
                  log.info("WALDORF_BALANCE_CHECK: user={} balance={}", recipient.getName(), recipient.getBalance());
              }
          }
          JAVAEOF
          sed -i "s|PLACEHOLDER|${PACKAGE}|g" "${JAVA_DIR}/component/DatabaseConduit.java"
          echo "DatabaseConduit.java written (includes save() + process())"

      # ── 10. Write TransactionKafkaListener.java ────────────────────────────
      - name: "Write component/TransactionKafkaListener.java"
        env:
          PACKAGE: ${{ steps.discover.outputs.package }}
          JAVA_DIR: ${{ steps.discover.outputs.java_dir }}
        run: |
          cat > "${JAVA_DIR}/component/TransactionKafkaListener.java" << 'JAVAEOF'
          package PLACEHOLDER.component;

          import PLACEHOLDER.foundation.Transaction;
          import org.slf4j.Logger;
          import org.slf4j.LoggerFactory;
          import org.springframework.kafka.annotation.KafkaListener;
          import org.springframework.stereotype.Component;

          @Component
          public class TransactionKafkaListener {

              private static final Logger log =
                      LoggerFactory.getLogger(TransactionKafkaListener.class);

              private final DatabaseConduit databaseConduit;

              public TransactionKafkaListener(DatabaseConduit databaseConduit) {
                  this.databaseConduit = databaseConduit;
              }

              @KafkaListener(
                      topics  = "${general.kafka-topic}",
                      groupId = "midas-consumer-group"
              )
              public void listen(Transaction transaction) {
                  log.info("MIDAS_AMOUNT: {}", transaction.getAmount());
                  databaseConduit.process(transaction);
              }
          }
          JAVAEOF
          sed -i "s|PLACEHOLDER|${PACKAGE}|g" "${JAVA_DIR}/component/TransactionKafkaListener.java"

          echo "=== Final component/ contents ==="
          ls -1 "${JAVA_DIR}/component/"

      # ── 11. Build ──────────────────────────────────────────────────────────
      # continue-on-error: true so the parse step always runs and produces
      # task3_answer.md even when the build fails — prevents the
      # "cat: task3_answer.md: No such file or directory" error on summary.
      - name: "Maven clean install (skip tests)"
        id: build
        run: mvn clean install -DskipTests --no-transfer-progress 2>&1 | tee
          build_output.txt
        continue-on-error: true

      # ── 12. Run TaskThreeTests ─────────────────────────────────────────────
      - name: "Run TaskThreeTests"
        if: steps.build.outcome == 'success'
        run: |
          mvn -Dtest=TaskThreeTests test --no-transfer-progress \
            2>&1 | tee test_output_task3.txt
        continue-on-error: true

      # Ensure test_output_task3.txt exists even when build failed
      - name: "Ensure test output file exists"
        if: always()
        run: |
          if [ ! -f test_output_task3.txt ]; then
            cat build_output.txt > test_output_task3.txt 2>/dev/null || echo "Build failed — no test output" > test_output_task3.txt
          fi

      # ── 13. Parse waldorf balance → task3_answer.md ────────────────────────
      - name: "Parse waldorf balance → task3_answer.md"
        if: always()
        run: |
          python3 << 'PYEOF'
          import re, math, textwrap
          from pathlib import Path

          log_file = Path("test_output_task3.txt")
          raw = log_file.read_text(errors="replace") if log_file.exists() else ""

          print(f"Log file size: {len(raw)} chars")

          # ── 1. Our own WALDORF_BALANCE_CHECK tag ──────────────────────────
          hits = re.findall(
              r"WALDORF_BALANCE_CHECK:\s*user=waldorf\s+balance=([\S]+)", raw, re.IGNORECASE
          )
          final_raw = hits[-1] if hits else None

          # ── 2. Any log line containing "waldorf" and a decimal number ─────
          if not final_raw:
              for line in reversed(raw.splitlines()):
                  if "waldorf" in line.lower():
                      print(f"  [waldorf line] {line.strip()}")
                      nums = re.findall(r"[\d]+\.[\d]+", line)
                      if nums:
                          final_raw = nums[-1]
                          break

          # ── 3. Search the BEGIN…END snippet ───────────────────────────────
          snippet_m = re.search(r"(BEGIN.*?END)", raw, re.DOTALL)
          snippet   = snippet_m.group(1).strip() if snippet_m else None
          if snippet:
              print(f"\n[BEGIN…END snippet found]\n{snippet}\n")
              if not final_raw:
                  nums = re.findall(r"[\d]+\.[\d]+", snippet)
                  if nums:
                      final_raw = nums[-1]

          # ── 4. Print ALL lines containing a balance keyword for diagnosis ─
          print("\n=== Lines containing 'balance' or 'waldorf' ===")
          for line in raw.splitlines():
              low = line.lower()
              if "balance" in low or "waldorf" in low:
                  print(f"  {line.strip()}")

          # ── 5. Print tail of log (where test results usually appear) ──────
          tail = "\n".join(raw.splitlines()[-150:])
          print(f"\n=== LAST 150 LINES OF LOG ===\n{tail}")

          # ── Build answer ──────────────────────────────────────────────────
          if final_raw:
              try:
                  val     = float(final_raw.rstrip(',;)'))
                  floored = math.floor(val)
                  answer_block = f"""
          ### waldorf's final balance

          | | Value |
          |---|---|
          | Raw (float) | `{val}` |
          | **Rounded down (floor)** | **`{floored}`** |

          > ✅ **Submit this value: `{floored}`**
          """
              except ValueError:
                  floored = "parse error"
                  answer_block = f"Found text `{final_raw}` near waldorf but could not convert to float."
          else:
              floored = "not found"
              answer_block = """
          ⚠️  Automatic parsing could not find waldorf's balance.

          **Check the Actions log for the section:**
          `=== Lines containing 'balance' or 'waldorf' ===`
          and
          `=== LAST 150 LINES OF LOG ===`

          These are printed directly to the Actions step output above — no download needed.
          Find the last balance value next to waldorf and apply `math.floor()`.
          """

          snippet_section = f"""
          ## BEGIN…END snippet

          ```
          {snippet}
          ```
          """ if snippet else "*(No BEGIN…END block found in test output)*"

          # Full log — NO truncation this time
          md = f"""# Midas Task 3 — Answer

          ## waldorf's balance after all transactions

          {answer_block}

          ---

          {snippet_section}

          ---

          ## Files created / modified

          | File | Path | Action |
          |------|------|--------|
          | `TransactionRecord.java`        | `entity/`             | Created — JPA entity |
          | `TransactionRepository.java`    | `repository/`         | Created — Spring Data repo |
          | `DatabaseConduit.java`          | `component/`          | Written — `save()` + `process()` |
          | `TransactionKafkaListener.java` | `component/`          | Rewritten — wires DatabaseConduit |
          | `application.yml`               | `src/main/resources/` | Updated — H2 + JPA config |
          | `TransactionListener.java`      | `component/`          | **Deleted** — wrong filename |

          ---

          ## Full test output

          <details>
          <summary>Click to expand (full, untruncated)</summary>

          ```
          {raw}
          ```

          </details>
          """

          Path("task3_answer.md").write_text(textwrap.dedent(md))
          print(f"\ntask3_answer.md written. waldorf balance = {final_raw} → floor = {floored}")
          PYEOF

      # ── 14. Commit ─────────────────────────────────────────────────────────
      - name: "Commit generated files"
        if: always()
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add pom.xml \
                  src/main/resources/application.yml \
                  src/main/java \
                  task3_answer.md 2>/dev/null || true
          git add -u src/main/java 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "Nothing to commit."
          else
            git commit -m "ci: Task 3 — DatabaseConduit save() + JPA integration [skip ci]"
            git push
          fi

      # ── 15. Upload artefacts ───────────────────────────────────────────────
      - name: "Upload artefacts"
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: "task3-results"
          path: |
            task3_answer.md
            test_output_task3.txt
            build_output.txt
          retention-days: 30

      # ── 16. Actions job summary ────────────────────────────────────────────
      - name: "Print summary"
        if: always()
        run: cat task3_answer.md >> "$GITHUB_STEP_SUMMARY"
